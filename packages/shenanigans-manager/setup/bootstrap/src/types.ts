{ { ^ shenanigans.game } }
/**
 * Settings to initialize a new {{ shenanigans.name }}.
 */
export interface  { { shenanigans.name } } Settings {
    // TODO: mplement me!
}

/**
 * {{ description }}
 */
export interface  { { shenanigans.name } } {
    // TODO: mplement me!
}
{ { /shenanigans.game }}
